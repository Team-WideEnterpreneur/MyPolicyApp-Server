spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://mydata-db.mysql.database.azure.com:3306/mydata?serverTimezone=UTC&characterEncoding=UTF-8
    username: mydata_user@mydata-db
    password: policy2020!
  jpa:
    show-sql: true
    hibernate:
      format_sql: true
      ddl-auto: update
server:
  servlet:
    encoding:
      charset: UTF-8
      force: true
      enabled: true
